{% extends 'base.html.twig' %}

{% block title %}Hello BookController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    <p>{{ view_exemple }}</p>

    {{ form(form) }}

    {% for book in books %}
        <div>
            <h2>{{ book.title }}</h2>
            <p>ISBN: {{ book.isbn }}</p>
            <p>Éditeur: {{ book.editor.name }}</p>
            <p>Statut: {{ book.status.label }}</p>
        </div>
    {% endfor %}



    This friendly message is coming from:
    <ul>
        <li>Your controller at <code>C:/Users/depri/Desktop/Symfony/biblios/src/Controller/BookController.php</code></li>
        <li>Your template at <code>C:/Users/depri/Desktop/Symfony/biblios/templates/book/index.html.twig</code></li>
    </ul>
</div>
{% endblock %}
